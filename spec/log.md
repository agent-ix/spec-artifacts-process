---
type: log
title: "Update Log"
description: "Chronological log of structural changes to this bundle."
---
# Update Log

## History

* **2026-08-26** — **FR-004 CR-043**: the measured `constraint` target and its
  `constraint-validation` reference now ship (`agent-ix/spec-artifacts-process#69`,
  `agent-ix/quire-rs#327/#328`). `required: false` preserves the FR contract's
  optional `Constraints` section without false `section-matches-nothing`
  findings; a present table still mints every `-CON-` row and carries its
  row-level locus. Bare `FR-nnn` tags remain non-minting and now receive
  model-grounded exact-child guidance. Activating the target also exposed and
  corrected FR-008's malformed three-column Constraints table. TC-078.

* **2026-08-26** — **FR-006 CR-042**: `suite` and `inspection` declare
  `evidence: reference-only` (`agent-ix/quire-rs#363`). Both registries exist so
  their stable ids can be referenced and checked for dangling links; neither
  row is an obligation that a source test can back. The first invariant-complete
  241-repository gap census exposed the conflation by routing `SUITE-001` to
  `authoring-absent`, even though suite headers are deliberately containers and
  not evidence. The posture preserves reference resolution while removing both
  id classes from coverage groups, totals, minted-target records and the census
  P3 denominator. TC-043.

* **2026-08-21** — **FR-002 CR-033**: `SpecReview.analysis` admits
  `architecture-evaluation`. Architecture scenario walkthroughs, trade-offs, risks and
  actionable findings use the existing validated review contract rather than a new
  artifact or score. TC-027 pins the complete vocabulary and drives a representative
  architecture review through Quire.

* **2026-08-24** — **FR-004 CR-041**: the written decision for the five id classes `agent-ix/spec-artifacts-process#69` names — `CON`, `INV`, `SC`, `VC` and bare `FR-nnn` (EPIC `agent-ix/quire-rs#264` Wave 3). Four are settled here; the fifth is settled as **mint** and BLOCKED, and the blocker is measured rather than asserted. **What the numbers count.** Unit: entries in `coverage.untracked_symbols`, one per `verifies` relation whose id is neither declared, referenced by a cell, nor any row's own id (quire-rs `coverage.rs:1247-1254`) — **not** deduplicated by id, so three tests naming `FR-030-CON-1` are three entries. Population: the **241** repositories `quire-rs/scripts/corpus.py` enumerates under `~/dev` (non-hidden top-level directories carrying `spec/`, minus `SKIP_DIRS`, `SUPERSEDED` and verified `-task<N>` worktree siblings). Instrument: **one** `quire` binary built from `quire-cli` at its pinned rev, engine `e06f121` (quire-rs `epic/264-detection-minting-integrity` HEAD), release profile, self-reported by every payload. Declaration: `qa-corpus/modules/ecosystem` @ `79398fc`, copies differing in exactly one declaration. Method: `quire coverage --scope <repo> --json` per repository, `untracked_symbols` and `unbacked_rows` read out of the payload — nothing re-implemented, which is the lesson of `agent-ix/quire-rs#309`. **The ticket's table was stale in three of five rows** and the three figures in circulation for it — 572, 322, 687 — disagreed with each other; none had been produced by the engine. Against a baseline of **5,309 / 20,030 rows backed = 26.51%** and **1,195** `untracked_symbols` entries: `CON` **155** entries / 28 repos, `SC` **124** / 7, bare `FR-nnn` **212** / 30, `INV` **1** / 1, `VC` **0**. **`CON` — mint it, and the two halves of the model already say so.** `spec-artifacts-iso` declares the FR `Constraints` table in `body_extraction` with `id_column: ID` and `id_pattern: '^{id}-CON-\d+$'`, so the *shape* contract validates `FR-001-CON-1` while the *traceability* model mints nothing for it. Resolved per id against each repository's `spec/`: **102 of the tagged `CON`/`INV` ids are defined by an `ID` row in an `## Constraints` table on an `FR` document**, 4 on an `NFR`, 1 under `## Invariants`, 1 in a matrix section only, and **19 name no row anywhere**. **Bad rule or bad corpus**, settled by opening files: sampled 12 of the 202 `CON`-in-test-file sites, one per repository across 12 of the 33 — **12 of 12 are genuine authored tags**, each asserting exactly the constraint its id names (`filament-parser-lib/tests/filament/tier1/test_frontmatter_extraction.py:109` over a test asserting `result.nodes[0].data == {}`; `identity/tests/test_mfa_model.py:148`; `platform-test-kit/tests/unit/test_capture.py:113`; `ts-auth-sdk/tests/AuthClient.device.test.ts:133`). Declaring the target and its `constraint-validation` reference is measured at rows minted **20,030 → 23,059 (+3,029)**, rows backed **5,309 → 5,452 (+143)**, `-CON-` untracked symbols **155 → 34 (−121)**, **2,693** constraint rows gaining a locus in `unbacked_rows`, `binding_census` **byte-identical** in all three languages (rust 2,804, python 2,050, typescript 1,684) and status lies unchanged at **904**. **The engine mints 3,029**, which is the same figure as the `+3,029` rows delta published above — and it must be, because baseline `unbacked_rows` carries **zero** `-CON-` rows, so nothing CON was minted before the declaration and the mint count and the delta are the same fact. An earlier draft of this note said an independent script counted **3,029** and that "instrument and script agree to the row". They do not agree, and 3,029 is not reachable: the target declares `archetype: FR`, so it cannot mint the `NFR` half that script counted. A careful re-census gives FR 2,854 / NFR 334. **The script figure is withdrawn; the engine's 3,029 stands, because it is the instrument.** **WHY IT DOES NOT SHIP YET.** Backing **26.51% → 23.64%** and unbacked rows **13,889 → 16,582** are the denominator becoming honest and are not the objection. The objection is **`section-matches-nothing` 512 → 1,672 (+1,160)**: `## Constraints` is *optional* in the FR contract and a `TraceTarget` has no way to say so — quire-rs states the opposite outright, that "a trace target's section is not optional" (`corpus/declared_tables.rs:69-73`). **Sampled 11 of the 1,160 across 11 repositories: 10 rule, 1 real.** Ten are FR documents legitimately without the section and without any `CON` id (`quire-rs/spec/functional/FR-003-archetype-schema-surface.md`, `ix-cli/spec/functional/core/FR-023-ix-logout.md`, `cloud-manager/spec/functional/FR-006-observability.md` among them); the one real find is `platform-test-kit/spec/functional/FR-009_isolated_deployment.md`, which writes `FR-009.2-CON-1` as an **H4 heading** rather than a table row. **The controlled corpus caught it, which is what it is for**: vendoring the two entries into `agent-ix/qa-corpus` and running it fails **25 of 75 cases with 40 mismatches** (it was 19 of 69 with 34 against the corpus BEFORE this change added six fixtures; the published pair mixed a pre-fixture numerator with a post-fixture denominator, and the six it adds fail under the declaration for exactly the same reason, 100% of them `section-matches-nothing`** — 13 asserting a silence that is no longer true, 6 asserting `diagnostic_paths` and receiving the wrong finding because a reason token is not scoped to the declaration that raised it, including `section-name-mismatch` and `section-and-id-column`, the flagship `#270` pair. Neither is repairable without LOWERING those assertions, and authoring a `## Constraints` table into 19 fixtures to dodge the finding is the corpus-side workaround CR-036 refused on principle. Filed as `agent-ix/quire-rs#327` — one key, `required: false`, mirroring the `body_extraction` key on the very table the target would mint from. The entries are written out verbatim in the manifest so the day #327 lands is a paste, not a re-derivation. **The reference ships WITH the target, never after it:** `unbacked_rows` is built inside `for declaration in &model.document_references` (`coverage.rs:1083`), so a target with no reference reading its table is a denominator with no locus. `constraint-validation` is the `verification` shape one section over and for the same stated reason: of the 3,029 minted rows only ~**363** carry a `TC`/`IT` id (`Unit Test` 566, `Integration Test` 382, `Test` 349, `Inspection` 329, `Code Review` 96), and a row naming no id is still answerable for its own `CON` id. **The other four are decisions in the manifest, not absences**, under one rule — *the traceability model mints from sections the shape contract declares and does not invent sections*. **`FR-nnn-INV-n` is NOT minted**: `## Invariants` is declared by neither module, 15 repositories author it anyway (129 rows) with the table literally `| ID | Constraint | Type | Rationale |`, and **one tag in the whole corpus names one of those ids** (`identity/tests/test_tenant_auth_policy.py:195`) — measured rather than assumed free, `section: [Constraints, Invariants]` moves 23,061/5,453 → 23,184/5,454, i.e. **123** denominator rows to reach one tag — an independent census counts 113 `FR` `## Invariants` `ID` rows across 15 repositories, consistent with it. An earlier draft published **328**, which was a figure in circulation alongside a "132" in this ticket's own comment; the shipped prose took the wrong one. The decision is unchanged — 123 rows to reach one tag is the same bad trade — but the magnitude was inflated 2.7x. **`NFR` `## Constraints` is NOT minted** for the same reason (4 tagged ids; that archetype declares no such section, so declaring it upstream in `spec-artifacts-iso` is the fix). **`IT-nnn-SC-nn` is unreachable and it is an engine limit rather than a decision**: those ids are list items — **1,356 occurrences across 28 repositories, 243 directly under `## Test Procedure`**, against 148 table occurrences, all 148 of which are *references* from a matrix rather than a minting table — so `agent-ix/quire-rs#244` owns it, as the manifest has recorded since CR-037. **`StR-nnn-VC-n` was already minted** by CR-037 (#63, closed 2026-08-22, before #69 was filed) and **the class has no population at all**: 36 rows minted in `filament-ide-rs`, 22 in `quire-rs`, 18 in `ix-cli`, backed **0** in every one, because not one `StR-nnn-VC-n` tag exists anywhere in any declared form — a fixture for it would have landed GREEN and taught nobody anything, so the corpus inventory row is corrected rather than covered. **Bare `FR-nnn` is NOT minted, deliberately**: 212 entries in 30 repositories, and a rule joining `FR-006` to `FR-006-AC-1..7` would back every criterion of a requirement from one test naming the requirement. It is not a `#307` near miss either — that ticket pairs ids differing by zero padding, case or separator, and these differ by a whole id segment. The diagnostic that should say "`FR-006` is not a trace target; use `FR-006-AC-n`" is not expressible by a module and is filed as `agent-ix/quire-rs#328`. **None of these tags vanishes for `agent-ix/quire-rs#312`'s reason when it sits on a test function** — the census counts every one and counts it as bound. The 275 `CON` sites reaching neither channel are that separate population: 229 in **non-test files**, where every enclosing symbol is production, and 46 at module scope or on a helper. #312 keeps its fix. **Deliberately not bundled, measured first:** adding `constraint` to `traces-to.targets`. **313 `Test Case Summary` rows across 18 repositories** already write a `CON` id in `Traces To` (`config-service` `TC-017` → `FR-003-AC-1, FR-003-CON-1`), so the population is real — but with that one list changed and nothing else, **every field of the payload is identical across all 241 repositories**. Zero movement, so it waits for a reason true independent of the count. FR-004-AC-17, TC-078.
* **2026-08-24** — **FR-004 CR-040**: TypeScript gains a **test-name form** — `typescript-test-name-id`, the analogue of `rust-test-name-id` (`agent-ix/spec-artifacts-process#68`, EPIC `agent-ix/quire-rs#264` Wave 3). TypeScript declared four trace forms and **none of them read a test's name**, so an id written where this ecosystem actually writes it — in the registered title — bound nothing at all. **What the numbers count.** Population: the **241** repositories `quire-rs/scripts/corpus.py` enumerates under `~/dev` (non-hidden top-level directories carrying `spec/`, minus `SKIP_DIRS`, `SUPERSEDED` and verified `-task<N>` worktree siblings); instrument: **one** `quire` binary built from `quire-cli` at its pinned rev, engine `08e5ed4` self-reported by all 482 payloads; variable: the module path, two copies differing in exactly one file and exactly one declared form. Registration census over `.ts`/`.tsx`: `it(` **5,106** sites, `test(` **1,167**, `describe(` **1,512**, `test.describe(` **120** — and **860 test registrations carry an id at the head of their title**, all 860 spelling the separator `-`, so `[-_]?` admits **zero** extra sites today and is there for the same reason CR-034's `_?` is. **Measured effect**, same binary, only the declaration changed — unit: Test Matrix rows and evidence symbols; population as above: rows minted **20,028 → 20,028** (unchanged, as a tag form must leave it), rows backed **5,044 → 5,308 (+264)**, backing **25.18% → 26.50% (+1.32pt)**, unbacked rows **14,403 → 13,889 (−514)**, status lies **1,104 → 904 (−200)**, typescript evidence symbols bound **1,043/6,199 (16.8%) → 1,685/6,199 (27.2%) (+642)**. Rust (**2,804/6,307**) and Python (**2,050/12,741**) binding are **byte-identical before and after**, which is the half that says the change is the one declared. **7 of 241 repositories change their backed-row count**; **12** change on at least one published metric, because three of the six that gain untracked symbols gain no backed row. An earlier draft of this sentence read "move at all", which its own adjacent `+110 untracked in 6 repositories` refutes; `filament-ide-rs` alone goes backed **1,354/2,540 (53%) → 1,529/2,540 (60%)**, typescript bound **10/408 (2.5%) → 363/408 (89.0%)**, unbacked **468 → 182**, status lies **209 → 55**, untracked symbols **25 → 25 (+0)**. **The ticket's own baseline differs and the reason is `agent-ix/quire-rs#322`**, which landed hours before this: #68 measured `ts 19/524 → 465/524` on an engine where `test.describe(` registered a `TestFunction`, so 116 suite headers sat in that denominator and ~102 of them bound their header tag *as evidence*. #322 made them `Container`s. Both readings land on 89%; the honest one is over the corrected pool. **The number that gets worse: untracked symbols 1,086 → 1,196 (+110)**, in 6 repositories. **Settled by opening them, not by the delta — sampled 12 sites across all 6, 0 invented bindings.** `ui-workflow-pane` `TC-048`…`TC-056` are real tests (`it('TC-048: WorkflowList renders grid of cards')`) whose Test Case Summary stops at `TC-047` — genuine orphans, the CR-034 `+13` class. `ix-cli` `TC-421` and `filament-editor-gateway` `TC-030` are named in a *Functional Coverage* table and minted by no Test Case Summary row. `ts-observability` declares `TC-006`/`TC-007` under a Test Case Summary whose id column is `` `ID` `` rather than the declared `Test ID`, so it mints nothing. `user-admin-ui` (49 of the 110) has **no `## Test Case Summary` at all**. Every one is a real tag the binder previously could not read at all; the channel is new information, not noise. **Bad rule or bad corpus, recall direction: sampled 12 of the 154 `filament-ide-rs` rows that stopped being status lies, 12 rule, 0 real** — TC-974, TC-454, TC-1031, TC-1176, TC-1018, TC-844, TC-1351, TC-664, TC-657, TC-1094, TC-965, TC-845, each with a real Playwright or vitest test whose title matches the row's stated claim. **Precision: the anchors are the whole design, and they are not the pattern #68 sketched.** That sketch named `describe` outright and reached `test.describe(` through its `(?:\.\w+)?` window; measured, it matches **148 sites the shipped form refuses — 110 suite headers and 38 suffixed or dashed sub-ids**. A suite header is not evidence (quire-rs CR-119/#322), **226 real corpus suite headers carry an id** — 148 bare `describe(` and 78 `test.describe(`, agreeing with `manifest.yaml`'s own census, which an earlier draft of this note contradicted with 224/146 in the same change. The bare count is method-sensitive: a broader predicate over every `.ts`/`.tsx` rather than test files gives 176. The `78` is stable across every predicate tried, and **the count is not what the claim rests on** — the zero below is, and since `regex` has no lookaround the modifier chain is an **allowlist** so `describe`/`suite` are structurally unable to appear. **[RAN]** those headers verbatim into one scope with a matrix declaring all 188 ids: backed **0/189**, typescript bound **0/225**, while a positive-control `it("tc-503: …")` in the same tree backs exactly 1 — so the zero is a measurement, not an absent harness. A second scope checks the container-span route: a `test.describe("tc-888: …")` wrapping `test("tc-999: …")` backs `TC-999` and leaves `TC-888` unbacked. Line-start anchoring mirrors `typescript.rs::registration`; the **trailing delimiter** terminates the id rather than truncating it, because `ix-cli` declares `TC-092` and `TC-280c` as separate rows and `filament-view-review` declares `TC-008-LIST` with no bare `TC-008` — a greedy `(\d+)` would mark those rows verified by tests that do not verify them. **The `id_format` hazard is INHERITED and stated rather than discovered:** `TC-{1}` renders captured digits verbatim exactly as the Rust form does, so `it("tc-1: …")` mints `TC-1` against a row declaring `TC-001` — that is `agent-ix/quire-rs#307`, unchanged in either direction, and zero-padding is not expressible in `id_format`. Measured, it produced **0** short ids in this sweep. **Deliberately out of reach:** the curried `it.each([…])("tc-503: …")` title (**0 of 37 `.each` sites carry an id**), and the 61 ids in attached comments and 45 in file-level docblocks #68 counts, which need `agent-ix/quire-rs#273`'s symbol rather than a title form. FR-004-AC-16, TC-077.

* **2026-08-22** — **FR-003 CR-039**: the matrix layer gains its **index half** — a `TestMatrixIndex` archetype (`agent-ix/spec-artifacts-process#62`). A repository past a certain size does not have one matrix; it has a **tree**, and `agent-ix/filament-ide-rs` says so in its own `CLAUDE.md` and in the document's Overview: the root indexes 12 module matrices and the detail lives one level down. The archetype modelled only the leaf, so after bringing all 12 module matrices onto the contract the root was the only remaining `[missing]` — on `test_cases` and `functional_coverage`, **neither of which it can honestly carry**. There was no way to clear that without inventing content: copying the 1,475 module test cases up would duplicate every row and give the same TC two declaring matrices, which `check_artifact_ids.sh` rejects and which makes a tracking tag bind to *neither*. **A separate archetype, not a `role:` discriminator** — #62 offered both and preferred this one, and it is also the only one expressible, because `body_extraction` carries a single `yield_pattern` per archetype and cannot branch on a frontmatter value (the same wall CR-037 hit). **Two things the split buys that a variant would not:** an index mints no test-case ids, because `trace_targets.test-case` binds `archetype: TestMatrix` and this is not one — under the old typing the root was a *minting document that minted nothing*; and the index's own structure is asserted for the first time, which matters because #62 records that this repository's root matrix had a subsystem row **missing its `Local Matrix` cell**, three columns where every other row had four, and no validator caught it. The integration matrix (`INT-NNN`) and coverage-gap register (`GAP-NNN`) are **optional**: a repository whose subsystems do not integrate has no `INT-` rows, and an absent register and an empty one are different claims — requiring either would force an empty table, the CR-018 mistake. Adoption is a one-line corpus change. FR-003-AC-12, TC-076.

* **2026-08-22** — **FR-004 CR-038**: the **doc-comment forms gain a trailing delimiter** (`agent-ix/spec-artifacts-process#64`). The anchor stopped an id binding from the middle of a sentence and never stopped a sentence that *begins* with one. Reproduced: `/// TC-1059 used to assert that …` — a note recording that TC-1059 was **retired** — read as a tag on the test below it. The dangerous mirror image is `/// FR-069-AC-8 is not covered here`, which marks a criterion **backed** by a test that says in plain English it does not verify it. **[RAN] before changing it**, as #64 asked. **What the number counts:** `///` id-leading lines in `tests/**` across `~/dev`, worktrees and `-task<N>` copies excluded — the tree where symbols actually bind, since only evidence symbols carry `verifies`. Current form **444**; #64's proposed delimiter **386** (drops 58); shipped form **390** (drops 54). **Sampled the 57 unique dropped lines one at a time: ~55 rule, 2 corpus** — nearly every one is narrative (`/// NFR-026 "no recreate, no close on move" guarantee is directly observable.`, `/// FR-098-AC-1 previously cited TC-1210 …`), each manufacturing a binding today. **The shipped form differs from #64's by admitting a trailing period**: four dropped lines were unambiguously authored tags (`/// NFR-035.`, `/// NFR-036.`, `/// FR-087-AC-5.`, `/// FR-068-AC-16.`) and `.` was not in the proposed class; it admits nothing new, because a period only counts immediately after the id. **The one real convention loss, recorded rather than hidden:** `/// TC-892 regression (FR-049, CR-069): …` in quire-rs — id, lowercase word, delimiter — which no pattern separates from prose; those two sites are swept onto `/// TC-892: regression (…)`. FR-004-AC-15, TC-075.

* **2026-08-22** — **FR-004 CR-037**: `trace_targets` declares **`stakeholder-validation-criterion`** (`agent-ix/spec-artifacts-process#63`). `StR-NNN-VC-N` ids are minted and a tag naming one reconciled against nothing, landing in `untracked_symbols` permanently with no authoring-side fix. Ships with CR-035 deliberately: `Analysis` and `Demonstration` are the methods those rows declare, so without the vocabulary the target would mint ids whose rows have no valid `Type`. **IT and US are absent, and that is an engine limit rather than a decision** — a `TraceTarget` mints from `section` + `id_column`, i.e. a table column, and `IT-NNN-SC-NN` are bullet items under `## Test Procedure` while `US-NNN-EX-N` are H3 headings. Declaring either would be a declaration the engine cannot honour, which is the CR-081 shape this ecosystem has hit four times. Filed as `agent-ix/quire-rs#244`; the third of the three lands now rather than waiting behind the other two. FR-004-AC-14, TC-074.

* **2026-08-22** — **FR-004 CR-036**: the `Traces To` pattern admits a lone **`-`** as the explicit no-trace form (`agent-ix/spec-artifacts-process#61`). A retired row has no live requirement to trace to — withdrawing it is precisely the act of severing that edge — and the pattern admitted nothing valid to write. **[RAN]** `agent-ix/filament-ide-rs` @ `fc5d644`: 16 rows across 8 module matrices carry `Traces To: -`, and **16 of 16 are retired** (`⛔`); zero are live rows with a missing trace. **Only the ASCII hyphen**, though #61 offered `—` and `n/a` too: all 16 measured rows write `-`, and `test_manifest.py` has pinned `—` as *rejected* since CR-017 — admitting a form no corpus authors, against a standing decision, to close a ticket is the widening this repo's guidance forbids. **Not coupled to the `Status` cell** as #61 suggested: `column_patterns` evaluates one cell in isolation and cannot see a sibling column. A live row carrying `-` is a real gap this does not catch; it is caught one layer over, as an unbacked row. Deliberately not worked around corpus-side — writing a plausible id into a retired row would fabricate an edge to pass a validator, and every review built on that matrix would then assert a trace that does not exist. FR-004-AC-13, TC-073.

* **2026-08-22** — **FR-004 CR-035**: `test_type` gains the three ISO 29148 **verification methods** — `Inspection`, `Analysis`, `Demonstration` — and `no_source_symbol` gains the first two (`agent-ix/spec-artifacts-process#60`). Every pre-existing value names a *harness kind*; a 29148-aligned corpus also authors verification by inspection, analysis and demonstration, and there was no declared value for any of them. **What the number counts:** `[assert]` findings from `quire validate --scope . "spec/**/*.md" "plan/**/*.md" "reviews/**/*.md"` over `agent-ix/filament-ide-rs` @ `fc5d644` — **496 findings, 453 rejected `Type` cells, of which 128 named a means the vocabulary had no word for** (`Inspection` 112, `Analysis` 11, `Demonstration` 5). Rows read rather than assumed: TC-085 *"Inspect Core for domain behavior ownership drift"*, TC-323 *"Inspect the packaged artifact for the pinned sidecar runtime"*. `Manual` is the nearest declared value and is wrong — those are automated scripts in a CI lane, not people. **The remaining 325 are case variants and synonyms and are a corpus problem**, swept in the consuming repo, not admitted here. **The catalog already carried the distinction one level up**: `verification_catalog` entries declare `class:`, and the classes in use are `Test` (21 methods) and `Analysis` (8) — `test_type` enumerated only the `Test` class. **Consequence beyond the assert count:** because `Inspection` was undeclared, those rows were typed as something that demands a source symbol and coverage reported them as **status lies** — the whole substance of SR-152 FND-002, carried as an open question across SR-150 → SR-151 → SR-152 (TC-441, TC-446, TC-447). Those rows were not lying; no declared type said what they are. **`Demonstration` deliberately stays OUT of `no_source_symbol`**: a demonstration is observed running, so it can carry a symbol. The vocabulary has **three literal copies** (`test_type`, TestMatrix `Type`, SuiteRegistry `Evidence Kind`) pending `from_vocabulary` (quire-rs#146); all three move together and TC-034/TC-042 are the drift guards. FR-004-AC-12, TC-072.

* **2026-08-22** — **FR-004 CR-034**: `rust-test-name-id` gains an **optional separator** — `'\bfn (?i:tc)(\d+)_'` becomes `'\bfn (?i:tc)_?(\d+)_'` (`agent-ix/spec-artifacts-process#59`, epic `agent-ix/quoin#197`). **What the number counts:** function-definition sites in `agent-ix/filament-ide-rs` @ `3349cf8` (24 crates, ~143k LOC), counted by spelling, under `quire 0.29.0` / engine `quire-rs v0.42.0` / this module at v0.23.0 — `fn tc_NNN_` **1,292**, `fn tcNNN_` **0**. Every tag written in that repository's dominant convention bound nothing. Changing this one pattern in a copy of the module took `quire coverage --scope .` from **555/2389 backed (23%)** to **1158/2389 (48%)**, unbacked rows 1538 → 528, status lies 761 → 265, with **no test written** — two thirds of everything reported as missing tests was the tool failing to read a tag that was present. **Bad rule, not bad corpus**, settled by opening documents rather than by the delta: **sampled 10 flipped rows, 10 rule, 0 real**, each with a real test carrying its id (TC-914, TC-1341, TC-216, TC-113 and six more). The argument is independent of the count — `fn tc744_x` and `fn tc_744_x` are the same token under the declared convention, and `(?i:tc)` already admits four spellings of it. **Precision does not move**: `_?` is anchored on both sides, adds no capture group and no list, and the +13 untracked symbols are all real orphan tests no matrix declares. **The comment that appeared to forbid this** (CR-024, "deliberately not widened") is about **list support**, which cannot apply to a form rendering `TC-{1}` over a function name; both notes now say which axis they mean. **Deliberately not fixed here:** the same repository's 643 `/// Tracing:` lines against a declared `Trace:` keyword — that one is bad corpus, measured at +56 backed for untracked symbols 32 → 200, and is swept in the consuming repo (`agent-ix/filament-ide-rs#460`). Three published SpecReviews there (SR-150/151/152) cite figures computed under the broken pattern. FR-004-AC-11, TC-071.

* **2026-08-20** — **FR-004 CR-032**: this module **declares** `traceability.source_exclude` (quire-rs FR-050-AC-22 / CR-085, `agent-ix/quire-rs#199`) — three globs, all anchored at a fixture directory. **`tests/**` must never appear on this key**: the `exclude:` globs on the trace targets *do* cover `tests/**` because that is where documents must not be read from, while `source_exclude` is the other root and the majority of any repository's trace markers live under `tests/` (194 of quire-rs's own ~458). Harmonising the two lists would delete the evidence tree. **The ordering edge demonstrated itself:** `TraceabilityModel` is `deny_unknown_fields`, so an engine that does not know the key **fails module load** rather than ignoring it — declaring it against the previously-installed toolchain turned 38 of 81 tests red with `unknown field 'source_exclude'`. The chain has **four** hops, not the three the plan assumed: quire-rs v0.41.0 → both quire-cli v0.28.0 *and* the `quire` Python wheel → spec-artifacts-iso v0.18.0 → this module. TC-070.

* **2026-08-20** — **FR-003 CR-031**: `⚠️` is **retired** from the `Status` vocabulary (agent-ix/spec-artifacts-process#52, agent-ix/quire-rs#192). The contract admitted it at `column_patterns.Status` while `traceability.status` classed it as **nothing** — and the comment on the first even named the second as the source of truth while admitting a value it does not define. Not cosmetic: `class_of` returned `Unknown`, the rollup's only consumer asked `== Complete`, and the row fell out of the report — not backed, not a lie, listed nowhere. A `⚠️` row was exempt from the status-lie check **by construction**, which is how six FR-016 rows in `quire-cli` claimed evidence they did not have while `status_lies` read zero. **Retired rather than classed**, per unify-means-enforce: `🚧` already means pending and every measured `⚠️` cell reads `⚠️ Partial: …` or `⚠️ Pending`, the same meaning in a second form. **What the number counts:** rows under `## Test Case Summary`, column `Status`, in a `type: TestMatrix` document, across the 239 `~/dev` repositories — **20 rows in 5 repositories** (18 `⚠️`, 2 `🟡`); only **10 rows in 3 repositories** validate cleanly today and therefore gate the enforcing release. **The guard that should have caught this now does**: `test_column_vocabularies_have_one_source` asserted one direction only, and was confirmed to fail against the pre-change manifest (`admitted but not classed: ['⚠️']`) before the manifest was touched. A drift guard that cannot fail is the defect it exists to prevent. TC-008, TC-069.

* **2026-08-20** — **FR-007 CR-030**: `concolic-execution` drops `structured-input`, one release after CR-029 added it. It is what `grammar-based-fuzzing` is keyed on, so both methods were recommended on **identical evidence** and a reader had no basis to choose between them — while one is an escalation of last resort and the other is not. **[RAN]** `quoin advise` over quire-rs: **17 of 18** concolic recommendations came from `structured-input` and one from `reference-equivalence`. The count is not the argument; the argument is that a trigger shared with a cheaper method answering the same question distinguishes nothing. What survives are the values that separate the two: the wall a fuzzer cannot climb (`magic-value-comparison`), the mandate (`high-criticality`), the two proof obligations (`secret-dependent-branch`, `reference-equivalence`), and the plateau (`fault-detection-unmeasured` / `fault-detection-failed`). Found by running the advisor over a real corpus rather than by reading the entry.

* **2026-08-19** — **FR-007 CR-029**: `concolic-execution` becomes **reachable**, and the evidence-side characteristic is **renamed**. It was keyed on `path-sensitive` and `hard-to-reach-branch`, and nothing could produce either: both name the *implementation's* control flow, and a specification states what the system must do, never that a branch behind it is hard to reach. Measured across this catalog it was **the last of 33 methods no requirement could elicit** (agent-ix/quoin#128 took that count from 7 to 1).

  **The re-key follows how the technique is actually reached.** Nobody starts there — it path-explodes and it is slow. The industrial pattern is *hybrid fuzzing*: fuzz until the coverage curve flattens, hand the stuck branches to a solver, feed the solved inputs back as seeds (Driller 2016, then QSYM, SymCC, Fuzzolic). Four documented reasons people reach for it, every one observable: a stalled campaign (`fault-detection-unmeasured` / `fault-detection-failed`, from the evidence store), a safety standard mandating path coverage — DO-178C Level A MC/DC, ISO 26262 ASIL D, IEC 62304 Class C — (`high-criticality`, from the obligation's own value), constant-time code (`secret-dependent-branch`), and equivalence with a reference implementation (`reference-equivalence`). Plus `magic-value-comparison` — a checksum, CRC, HMAC or magic number, the classic wall a fuzzer cannot climb and a solver clears in one step — and `structured-input`. `haybale` joins the tooling: symbolic execution of LLVM IR written in Rust (crates.io 0.7.2), so it applies to anything rustc compiles.

  **`suite-quality-unknown` → `fault-detection-unmeasured`.** "Quality" over-claimed: a suite can be slow, flaky or unreadable and none of that is what this measures. "Suite" named the wrong subject: the fact is about *this obligation's* evidence, and the same suite may have a measured score for one obligation and none for another. Declared once and produced by nothing, so the rename was free — it will not be once it fires. Its counterpart is `fault-detection-failed`. **Deliberately not `surviving-mutants`**: that names mutation testing's artifact while `concolic-execution` reads the same signal and produces no mutants — CR-015's `evidence_kind` reasoning, one axis over.

  **Deliberately absent: a `fuzz-plateau` value**, the literal Driller trigger. Nothing records coverage over time and a proxy invented to fill the gap is the CR-014 failure — an open set whose membership had to be judged rather than read. **And what the entry still cannot say** is that it is an escalation of last resort: `advise` ranks by how many rules matched, so this now outranks `unit-testing` on a statement matching two triggers with nothing marking the difference. Filed as agent-ix/quire-rs#190; until it lands the guidance lives in quoin's `spec-evidence-analysis` and `spec-fuzz` skills, which is the skill-local-prose arrangement ADR-0011 moved away from — recorded as a debt rather than left to be rediscovered.

  AC-13/AC-14 and TC-067/TC-068 are **denylists, and that limit is deliberate**. Two general forms of AC-14 were written and both were worse than nothing: "does a declared tool name appear inside the value" passes `surviving-mutants` cleanly, because no tool is called *mutants*; reversed, it fires on `cross` in `crosshair` and `fault` in `fs-fault-injection`. A check that needs judgement is the failure this catalogue keeps citing, so the principle is stated in the CR and the tests guard the specific mistakes.

* **2026-08-19** — **FR-004 CR-028**: the traceability model declares **`trace_tags.implements`** (quire-rs FR-062) — the marker forms binding **production code** to the requirement it is about. `verifies` links an evidence symbol to a trace id; nothing linked a requirement to the code that implements it, so mutation scoping (quoin FR-039) had no file set to mutate. **[RAN]** across quire-rs's 52 functional requirements: **38 had at least one mutable target, 14 had none**, and the fourteen fail for one reason — every symbol verifying them lives in `tests/`. Reach correlated with **test placement**, not requirement quality (quire-rs CR-071). A separate list rather than a flag on `markers`, because `markers` mint evidence and these mint scope, and a shared list with a discriminator field puts one typo between the two; the engine keeps them apart structurally by binding `markers` to test/benchmark/fuzz symbols only and `implements` to functions and containers only, so a mis-declared form binds nothing rather than the wrong thing. A comment form (`/// Implements: FR-001, FR-002-AC-3`) rather than an attribute, since `#[implements("…")]` would need a proc macro in every consuming crate and the point is annotating production code that already exists. The literal `Implements:` keyword is the prose guard that the `*-comment-id` forms need a trailing delimiter for.

  **Declaring this block is what surfaced quire-rs CR-081.** v0.36.0 minted the relation and v0.37.0 carried it into `coverage --json`, but a module's declared forms were dropped between the manifest and the binding by two hand-maintained per-field functions (`merge_traceability` and `TraceabilityModel::is_empty`), so against v0.37.0 this declaration loads and mints **nothing** — the fourth instance of the engine-before-module ordering gap in this programme. Requires quire-rs **v0.38.0** / quire-cli **v0.26.0**. FR-004-AC-10, TC-066.

* **2026-08-18** — **CR-006**: new **FR-008** adds an optional **`Escape Cause`** column to the `SpecReview` findings table (agent-ix/spec-artifacts-process#36). A defect that is found got past everything meant to catch it; escape cause records **which layer let it through**, and nothing else in this module does. Severity says how urgently to look; a defect count says how many there were; neither says whether the specification, the evidence, or the implementation is leaking. Four values: `missing-requirement` (nobody wrote it), `wrong-requirement` (written, said the wrong thing), `correct-requirement-no-evidence` (right requirement, nothing verified it), `implementation-bug-despite-evidence` (right requirement, real evidence, wrong code). The fourth is the one to watch — the only cause that does not indict the spec, and the only one a green Test Matrix would have called healthy.

  **The first attempt put it on the wrong artifact, and the ticket pointed there.** #36 says the `Finding` archetype is a stub to be given a body contract and wired into the review skills. Both halves are false. **[RAN]** over `~/dev`: **169 `SpecReview` documents, 90 `Review` documents, and 2 typed `Finding` — both of which are mistyped analyses** (`AN-002 "Spec Integrity Analysis"`). Nothing has ever authored a real Finding. And `SpecReview` was created on 2026-06-20 *deliberately beside* the freeform `Review` archetype — "NOT overloading freeform `Review`, which is used ecosystem-wide with heterogeneous bodies — would break" — precisely so the analysis skills would not touch the `Review`/`Finding` container-and-child path. A contract on `Finding` would have attached the axis to a form nobody produces, and wiring the skills to it would have reversed a shipped decision. The ticket was written in an August research pass that did not know about the June one. CON-3 now states that the skills keep emitting SpecReview.

  **Optional, in two senses.** Declared in `optional_columns` (CR-023), so the ordered-subsequence rule keeps every existing four-column findings table valid — a required fifth column would have invalidated 169 documents the day it shipped. And optional because **not every finding is an escaped defect**: a completeness observation has no layer that leaked, and forcing a value would manufacture data.

  **The `Finding` id defect, fixed without giving it a contract.** `id_pattern: Finding-{next:03d}` minted `Finding-001`, which fails the archetype's own frontmatter schema `^[A-Z]{2,4}-[0-9]+$` — seven letters is not two-to-four — so **every id it produced was invalid** and a Finding authored the standard way could not validate. Now `FIND-{next:03d}`. Deliberately **not** `FND-`, which is the namespace of the findings *rows* inside a SpecReview: a Finding is a document and a child of `Review`, and sharing the prefix would read as unification and be a conflation. TC-063 asserts both properties — that the minted id validates, and that it does not fall in the row namespace. Matrix: TC-060..TC-064.
* **2026-08-18** — **CR-007**: `fault-injection` gains the applicability signal `object_types: [hazard, failure_mode]` (agent-ix/spec-objects-security#5). A declared failure mode names a failure the system can suffer and a hazard names a state that failure reaches — which is the applicability question this method asks, already answered in the document. **No method was minted**: `fault-injection` already existed and only the signal was missing, and `object_types` is the same axis `attack_surface` already uses to advise DAST/SAST/IAST, so this is one entry rather than an FMEA/HAZOP pair saying twice what the catalog says once. Matrix: TC-065. Renumbered from CR-006 to CR-007 on merge: FR-008's escape-cause column landed on `main` first and a published change id is not reused.

* **2026-08-17** — **CR-005**: two catalog entries added and the criticality question settled (agent-ix/spec-artifacts-process#43). quire-rs **v0.30.0** began reporting a declared method this catalog does not carry (FR-054-AC-11), and the first sweep — quire-rs' own 20 NFR `Measurement and Evaluation` tables, **55 rows, 17 distinct strings** — showed the `Method` column doing four jobs at once. Fifteen of the seventeen named something the catalog already had, under a different word: a **tool** (`Proptest`, `` `cargo-mutants` ``, `Criterion Benchmark`, `Fuzz Run`, `loom Exhaustive Interleaving` — and `deterministic-simulation` already lists loom in its own `tooling`), a **class synonym** (`Unit Test`, `Static Inspection`), or a **cadence** (`CI Gate` ×11, `Scheduled CI Gate` ×4, which say *when* a check runs and belong to the suite registry's schedule). FR-007 now carries the mapping table so a repository's sweep is a lookup rather than a judgement call, and **AC-10** stops this catalog making the same conflation the cells did.

  Two of the seventeen were real, and are added rather than forced into a near neighbour — a catalog whose nearest entry is wrong advises worse than one that admits the gap (**AC-9**, TC-056):

  - **`compile-time-check`** (Analysis / Compile) — the property is encoded so a violation does not build. `#![forbid(unsafe_code)]` is not `static-quality`, which measures a weakness over source, and not `design-by-contract`, which checks an executable annotation wherever it runs. There is nothing to run.
  - **`dynamic-analysis-sanitizer`** (Analysis / Integration) — TSAN, ASAN, Miri. Not `static-quality` (it executes), not `fuzzing` (it generates no input), not `fault-injection` (it induces nothing). The catalog had **no dynamic-analysis entry at all**, which is a real hole in a 29119-4-shaped registry rather than a quire-rs-specific need.

  **Criticality: already answered upstream.** This module declares no `criticality_column`, so every obligation the ecosystem derives carries none — and quoin's auditor hardcoded a `P0` multiplicity rule against it, a rule that could never fire. quire-rs [FR-053](ix://agent-ix/quire-rs/FR-053) already specifies the arrangement ("criticality is genuinely optional today … declaring one is a module's choice"), so the correction belonged in quoin rather than here. What is worth recording is *why* the column cannot simply be added: the ISO acceptance-criteria contract asserts exactly `ID | Criteria | Verification` with no `optional_columns`, and the engine's column assert is exact equality, so a fourth column fails validation — adding criticality is a `spec-artifacts-iso` change first, not an authoring habit. `Priority` exists only on the Test Matrix row, where it rates the **test** rather than the requirement. FR-007 records this and states that until the contract changes a consumer must not assume any obligation carries a criticality. Matrix: TC-056, TC-057.

* **2026-08-17** — **FR-007 AC-8**: the traceability model declares the ecosystem's **obligation sources** (quire-rs FR-053) — the FR and NFR acceptance-criterion targets, and the NFR `Measurement and Evaluation` table with rendered ids and its target/threshold parameters. **Found end-to-end, not by inspection**: `quoin evidence record` against a real repository bound *nothing* and reported every trace id as unmatched, because the engine's obligation machinery is inert until a module declares a source. It shipped, ran, and derived nothing. Consumed spec-artifacts-iso **v0.12.0**, whose FR-035 fixture gained `ObligationSource` (CR-004) after this declaration failed the schema gate — the third time that gate has caught a key the engine accepts and the contract had never heard of. Matrix: TC-055.

* **2026-08-17** — **FR-007** (new FR): the **verification-method catalog content** — 31 methods as module data conforming to quire-rs FR-054 (agent-ix/spec-artifacts-process#35, epic agent-ix/quire-rs#81 P1). Choosing how a requirement gets verified is the core of a testing plan, and that knowledge was prose in three disconnected places: IADT lived in two spec-artifacts-iso lint rules, techniques in `traceability.vocabularies.test_type`, and quoin's `spec-evidence-analysis` skill assigned a method per requirement from a **skill-local table that no manifest declared and no code read**. The seed set is the **full sweep**, not a starter subset — 21 Test, 7 Analysis, 1 Inspection, 2 Demonstration — because a catalog shipping half the techniques teaches the advisor the other half do not exist, and an absent entry reads as "not applicable" rather than "not yet written". `evidence_kind` is drawn from the declared `test_type` vocabulary and is **method-class-shaped, never per-tool**: the tool lives in the suite registry's own `tool` column, so semgrep, an SBOM generator and a DAST scanner need no vocabulary entries of their own — without that split every adapter quoin adds would push a new term into a vocabulary three other things already read. Applicability rules use this module's own axes (`property_shapes`, `characteristics`, `object_types`), which the engine stores and never interprets; they are what make the interesting advice reachable — an `attack_surface` object suggests DAST, temporal phrasing suggests runtime monitoring or model checking, a reliability NFR suggests fault injection, a concurrency shape suggests deterministic simulation. None of those are reachable from `Verification: Test` defaulted by habit.

  **The reconciliation #35 asked for is partial, and deliberately so.** The catalog single-sources the *method* axis for consumers, through quire-rs's derived `verification_method` / `verification_class` lookups. The *kind* axis is still three literal lists — catalog, Test Matrix `Type`, suite registry `Evidence Kind` — because `from_vocabulary` was deferred (quire-rs#146); TC-050 is what keeps them honest until it lands.

  **The FR-035 gate earned itself again.** Declaring the block failed this repo's own suite on `Additional properties are not allowed ('verification_catalog' was unexpected)` — the key the engine accepts and the contract had never heard of. Before iso CR-002 that check skipped in silence. Fixed upstream in spec-artifacts-iso **v0.11.0** (FR-001 CR-003) and consumed here. Matrix: TC-048..TC-054. Closes agent-ix/spec-artifacts-process#35.

* **2026-08-17** — **FR-006** (new FR): the **evidence-layer archetypes**, `SuiteRegistry` and `Inspections` — the authored half of the ADR-0011 evidence store (agent-ix/spec-artifacts-process#34, epic agent-ix/quire-rs#81 P1). The store splits by who writes it: authored → markdown validated as a document, machine-transcribed → JSON that is typeless and corpus-invisible by design. The JSON half needs no archetype and gets none; the markdown half needs both, because a suite registry nothing validates is a list of commands nobody checks. `SUITE-N` is a **structured, doc-scoped, never-renumbered** id rather than a kebab slug — everything joins on it (run directories, bindings, freshness) and a policy-immutable semantic string is exactly what produced the 1,014 dead trace tags (quire-rs#72); the suite's name and command live in their own columns, where they can change without breaking a join. `Evidence Kind` draws from the **same** declared `test_type` vocabulary as the Test Matrix `Type` column and the obligation record's method (CR-015: one vocabulary, three uses), with the tool in its own column so a tool-specific report — semgrep, SARIF, an SBOM — needs no vocabulary entry of its own. That vocabulary is **restated rather than referenced**, a known debt: `column_choices` takes a literal list and `from_vocabulary` was deliberately deferred (quire-rs#146), so TC-042 is what keeps the copies honest until it lands.

  **Both documents live under `spec/`, which corrects a premise in agent-ix/quoin#79.** That design places `evidence/` at the repository root on the grounds that "corpus membership is type-driven post-quire-rs#73, so quire validates them wherever they live". **[RAN]** it: quire-rs CR-045 bounds the document walk to `<scope>/spec`, and #73 made membership type-driven *within* the walked root, not everywhere on disk. A typed, well-formed `evidence/suites.md` at the repository root minted **nothing** and was reported nowhere; the identical file at `spec/evidence/suites.md` was walked, validated, minted its ids and had its references reconciled. A registry that looks authoritative while being checked by nothing is the "green matrix over dead links" failure reproduced inside the store built to prevent it. Filed on quoin#79 rather than silently overridden.

  Two smaller calls. Neither archetype declares `allowed_links`: these documents are joined by declared **trace** references, not edge verbs, and declaring `references` would have added two more `UnknownEdgeType` warnings to every repo on this module for nothing — measured at zero evidence-layer mentions for a non-adopting repo (TC-047). And the FR-003 scope guard was widened by two explicit entries rather than loosened, so a contract arriving by accident still costs a deliberate line. Matrix: TC-040..TC-047. Closes agent-ix/spec-artifacts-process#34.

* **2026-06-15** — Adopted OKF-compatible bundle structure with directory indexes.
* **2026-08-04** — Added `usecase/` (US-001) and FR-003 (TestMatrix `body_extraction`); restructured `tests.md` to the spec-matrix table shape.
* **2026-08-04** — FR-003 refined per review: `Priority` `column_choices` (P0–P4 vocabulary), widened `Traces To` tokens (CON rows, IT ids/success criteria), normalize-before-enforce rollout constraint (FR-003-CON-1).
* **2026-08-04** — Base spec review recorded (`reviews/base.md`, SR-001): fixed stale contribution counts in `spec.md` and marked US-001's open question resolved by FR-003; FR-003 matrix coverage and FR-002 coverage gaps left open.
* **2026-08-04** — Test Matrix completed for FR-003 per the six coverage rules: TC-001..TC-018 (vocabulary permutations, `min_rows`/table-presence boundaries, Test ID / Traces To / Status / Priority error paths), per-AC coverage rows, US-001 coverage, option-permutation/constraint-boundary/edge-case/gap sections (resolves SR-001 FND-001).
* **2026-08-04** — TDD plan bundle authored for FR-003 (`plan/Plan-001-testmatrix-body-extraction/`): 6 tasks, tracks A/S/Gate/C, FR-003-CON-1 modeled as a hard sign-off gate before the enforcing publish; no quire-rs engine work in scope.
* **2026-08-04** — All open SR-001 findings resolved (user-authorized, incl. pre-existing artifacts): FR-002 matrix coverage added (TC-019..TC-023, closes FND-002/GAP-001); StR-001 contribution counts corrected to 6 archetypes / 9 artifact types (FND-005); FR-003-AC-11 added for Test ID uniqueness with the missing quire-rs uniqueness assert recorded as an external dependency (GAP-002, TC-024 blocked).
* **2026-08-19** — FR-007 CR-027: the traceability model gains a **`configuration-matrix`** obligation source declaring `combinatorial` (quire-rs FR-061). A `## Configuration Dimensions` table on an FR states a configuration *space*, and the obligation is about the **interaction** of its rows — so unlike every other source here it mints ONE obligation for the table rather than one per row. `strength: 2` (pairwise; 3-way multiplies the target for a return nobody has measured), and an `Excludes` column because a space with forbidden combinations and no way to state them demands coverage of combinations that cannot exist. **This declaration is why quire-rs FR-061 and quoin FR-035 can fire at all**: both shipped in ADR-0011 Phase 2 wave D against a `module-manifest.schema.json` that rejected the key, so zero combinatorial obligations existed anywhere in the ecosystem — the third instance of the engine-before-module ordering gap in that program (spec-artifacts-iso FR-001 CR-008, iso v0.16.0). FR-007-AC-12, TC-055.

* **2026-08-15** — Added FR-005 declaring the Task `track` property (optional string, no enum) with TC-037/TC-038; 230 task files across the ecosystem carried the key through `additionalProperties: true` and nothing checked it. The competing nodal `Track` archetype (PR #9) is closed unmerged per filament-ide-rs SR-074 / #232 — a track is a Task property and the tree node is synthesized by the consumer (CR-026).
