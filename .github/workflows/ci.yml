name: CI

on:
  workflow_dispatch:
    inputs:
      publish:
        description: "Publish the package to pypi as well as running checks"
        type: boolean
        default: false

jobs:
  # Publishing is opt-in. Dispatching this workflow to run the checks used to
  # publish as a side effect, which is a hazard while FR-003-CON-1 keeps the
  # enforcing module version behind a sign-off gate: asking for a test run
  # would have shipped the thing the gate exists to hold back.
  publish-to-pypi:
    if: inputs.publish
    runs-on: ubuntu-latest

    steps:
    - name: Encode key to base64
      id: encode_key
      run: echo "gcp_key_64=$(echo -n '${{ secrets.GCP_SERVICE_ACCOUNT_KEY }}' | base64 -w 0)" >> "$GITHUB_OUTPUT"

    - name: Publish
      uses: agent-ix/python-service-actions/publish-to-pypi@main
      with:
        username: _json_key_base64
        password: ${{ steps.encode_key.outputs.gcp_key_64 }}
        registry_url: https://${{ vars.GCP_REGION }}-python.pkg.dev/${{ vars.GCP_PROJECT_NAME }}/${{ vars.GCP_PYPI }}

  ci:
    uses: agent-ix/python-service-actions/.github/workflows/lib-ci.yml@main
    with:
      extra_index_url: https://${{ vars.GCP_REGION }}-python.pkg.dev/${{ vars.GCP_PROJECT_NAME }}/${{ vars.GCP_PYPI }}/simple/
    secrets:
      GCP_KEY: ${{ secrets.GCP_SERVICE_ACCOUNT_KEY }}
